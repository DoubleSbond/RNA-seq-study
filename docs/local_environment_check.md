# Local Environment Check

Date checked: 2026-08-04

## Codex Workspace

- Workspace is writable outside `sources/`.
- `sources/` is treated as read-only reference material.
- No project files were found under `sources/` at the time this archive skeleton was created.

## Git

- Git is installed: `git version 2.53.0.windows.3`.
- Local repository was initialized in this workspace.
- Default branch is `main`.
- GitHub remote is configured: `https://github.com/DoubleSbond/RNA-seq-study.git`.
- Global Git user name was not configured in the checked environment.
- Global Git user email was not configured in the checked environment.

## Git LFS

- Git LFS is installed: `git-lfs/3.7.1`.
- Large files are ignored by `.gitignore` by default.
- Use Git LFS only for carefully selected large files that truly need to be versioned.

## GitHub CLI

- GitHub CLI (`gh`) was not detected in the current PATH.
- GitHub CLI authentication could not be checked because `gh` is unavailable.
- Next options:
  - Install GitHub CLI and run `gh auth login`.
  - Or use a normal Git remote URL and authenticate through Git Credential Manager when pushing.

## GitHub Codex Connector

- GitHub connector authorization was completed.
- Authenticated GitHub login: `DoubleSbond`.
- GitHub App installation was confirmed for account `DoubleSbond`.
- Repository access confirmed for `DoubleSbond/RNA-seq-study`.
- Repository permissions include push/admin access.
- Repository default branch: `main`.
- Repository visibility: public.
- Initial archive skeleton was published to GitHub through the connector.
- No GitHub organizations were listed by the connector at the time of checking.
- No tokens, credentials, or private authorization details were read or recorded.

## SSH and HPC

- OpenSSH is available: `OpenSSH_for_Windows_9.5p2`.
- `ssh`, `scp`, and `sftp` commands are available.
- An active local `ssh` process was detected.
- SSH config and key files were not inspected.
- No token, private key, password, or credential content was read or recorded.

## Remaining Inputs for Reproducible Archive Completion

- Any HPC project path, accession, DOI, or storage location safe to record in `data/README.md`.
- Exact command histories, scripts, and small result summaries to add under the archive structure.
